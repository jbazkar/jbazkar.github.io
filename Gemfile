source "https://rubygems.org"

# Use the GitHub Pages gem (bundles the right Jekyll + plugins)
gem "github-pages", group: :jekyll_plugins

# Explicitly include your theme (optional if already via _config.yml)
gem "jekyll-theme-cayman"
